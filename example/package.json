{
  "name": "example",
  "version": "1.0.0",
  "description": "example of electron zip",
  "main": "index.js",
  "scripts": {
    "dist": "$(npm bin)/electron-zip-packager ./ video-transcript-note --platform=all --arch=all --version=0.30.1"
  },
  "author": "azu",
  "license": "MIT",
  "dependencies": {
    "electron-zip-packager": "file:.."
  }
}
